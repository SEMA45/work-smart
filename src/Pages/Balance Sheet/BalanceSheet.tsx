import React from 'react'

type Props = {}

const BalanceSheet = (props: Props) => {
  return (
    <div
      className={`w-full h-fit min-h-[100%] overflow-hidden overflow-y-scroll bg-blue-100 p-4 relative`}
    ></div>
  );
}

export default BalanceSheet